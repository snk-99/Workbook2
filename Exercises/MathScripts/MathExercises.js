"use script"

//find your net worth
var assets = 60000000;
var debts = 500000;

var netWorth = assets - debts;

console.log(`My net worth is ${netWorth}`)



var radius = 8;
// math properties
var area = Math.PI * radius ** 2;

console.log(`The area of a circle with a radius of ${radius} is ${area.toFixed(2)}`)


// area of a rectangle
var length = 9;
var width = 7;

area = length * width;

console.log(`The area ot the rectangle is ${area}`)

//calculate tip amount
var foodBill = 40.78;
var tip = .20;

var tipAmount = foodBill * tip;

console.log(`The tip on $${foodBill} food bill is $${tipAmount.toFixed(2)}`)





